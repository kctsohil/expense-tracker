name: hive_database_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # hive database
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # format dates
  intl: 

  flutter_speed_dial: ^7.0.0


  confirm_dialog: ^1.0.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  # hive database generators
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true